import React from 'react';
import { StyleSheet, Text, View } from 'react-native';

const ToDestPresenter = () => {
  return (
    <View>
      <Text>ToDestPresenter</Text>
    </View>
  );
};

const styles = StyleSheet.create({});

export default ToDestPresenter;
