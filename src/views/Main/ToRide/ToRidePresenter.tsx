import React from 'react';
import { StyleSheet, Text, View } from 'react-native';

const ToRidePresenter = () => {
  return (
    <View>
      <Text>ToRidePresenter</Text>
    </View>
  );
};

const styles = StyleSheet.create({});

export default ToRidePresenter;
